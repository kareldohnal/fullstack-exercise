export const jwtConstants = {
    secret: 'VerySecretSecret', // TODO: Replace with env variable in production
    cookieEncryptionSecret: `verySecretSecretForCookies` // TODO: Replace with env variable in production
}
